using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

/*

public class BestillingsTjekker : MonoBehaviour


{
    public List<string> NPCordrer = new List<string>();
    public List<string> ordrer = new List<string>();
    public void Start()
    {

        GameObject GameManager = GameObject.Find("GameManager");

        NPCBestilling NPC_Bestilling = GameManager.GetComponent<NPCBestilling>();

        PlayerOrdrer Player_Ordrer = GameManager.GetComponent<PlayerOrdrer>();

        ordrer = Player_Ordrer.ordrer;
        NPCordrer = NPC_Bestilling.NPCordrer;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public static void Main()
    {
        bool isEqual = Enumerable.SequenceEqual(., y);
        if (isEqual)
        {
            Console.WriteLine("Lists are Equal");
        }
        else
        {
            Console.WriteLine("Lists are not Equal");
        }
   
        public void etellerandet()
        {

            GameObject GameManager = GameObject.Find("GameManager");

            NPCBestilling NPC_Bestilling = GameManager.GetComponent<NPCBestilling>();

            PlayerOrdrer Player_Ordrer = GameManager.GetComponent<PlayerOrdrer>();

            ordrer = Player_Ordrer.ordrer;
            NPCordrer = NPC_Bestilling.NPCordrer;
        }
        
}
    */